[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
pytest = "*"
ipytest = {editable = true, path = "."}

[dev-packages]
black = "==18.6b4"
invoke = "*"

[requires]
python_version = "3.7"

[scripts]
precommit = "invoke precommit"
format = "invoke format"
test = "invoke test"
